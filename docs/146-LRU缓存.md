# 146. LRU 缓存

## 题目描述

请你设计并实现一个满足  LRU (最近最少使用) 缓存 约束的数据结构。

实现 `LRUCache` 类：

- `LRUCache(int capacity)` 以正整数作为容量 `capacity` 初始化 LRU 缓存
- `int get(int key)` 如果关键字 `key` 存在于缓存中，则返回其对应的值，否则返回 `-1` 
- `void put(int key, int value)` 如果关键字 `key` 已经存在，则变更其数据值 `value` ；如果关键字不存在，则向缓存中插入该组 `key-value` 。当缓存容量达到上限时，它应该在写入新数据之前删除最近最少使用的数据值

## 解题思路

### 方法 1: 哈希表 + 双向链表（推荐）

使用哈希表快速查询节点，使用双向链表维护访问顺序。

- 虚拟头尾节点便于操作边界情况
- 最近使用的节点移到链表尾部
- 最少使用的节点在链表头部（虚拟头节点之后）

## 代码实现

```python
class Node:
    def __init__(self, key=0, value=0):
        self.key = key
        self.value = value
        self.prev = None
        self.next = None

class LRUCache:
    def __init__(self, capacity: int):
        self.capacity = capacity
        self.cache = {}
        self.head = Node()
        self.tail = Node()
        self.head.next = self.tail
        self.tail.prev = self.head
    
    def get(self, key: int) -> int:
        if key not in self.cache:
            return -1
        node = self.cache[key]
        self._move_to_tail(node)
        return node.value
    
    def put(self, key: int, value: int) -> None:
        if key in self.cache:
            node = self.cache[key]
            node.value = value
            self._move_to_tail(node)
        else:
            if len(self.cache) == self.capacity:
                # 删除最少使用的节点
                node_to_delete = self.head.next
                self._remove_node(node_to_delete)
                del self.cache[node_to_delete.key]
            
            new_node = Node(key, value)
            self.cache[key] = new_node
            self._add_to_tail(new_node)
    
    def _remove_node(self, node):
        node.prev.next = node.next
        node.next.prev = node.prev
    
    def _add_to_tail(self, node):
        node.prev = self.tail.prev
        node.next = self.tail
        self.tail.prev.next = node
        self.tail.prev = node
    
    def _move_to_tail(self, node):
        self._remove_node(node)
        self._add_to_tail(node)
```

## 复杂度分析

- **时间复杂度**：$O(1)$，`get` 和 `put` 操作均为常数时间
- **空间复杂度**：$O(\text{capacity})$，哈希表和链表共存储 `capacity` 个节点

## 相关题目

- 155. 最小栈
- 295. 数据流的中位数
